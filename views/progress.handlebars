<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Progress</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <h1>ZuluZest</h1>
  <h2 style="text-align: center;">Player Progress</h2>
  <div class="grid">
  {{#if playerProgress.length}}
    <ul>
      {{#each playerProgress}}
        <li>Level: {{this.level}}, Stage: {{this.stage}}, Complete: {{this.is_complete}}</li>
      {{/each}}
    </ul>
  {{else}}
    <p class="progress" style="text-align: center;">No progress data available.</p>
  {{/if}}
  </div>
</body>
</html>

